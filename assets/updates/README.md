# BALLY NEWS 영상

이 폴더에 영상 파일을 넣고, 메인 화면 BALLY NEWS 섹션에 노출합니다.

- 파일명을 `clip-1.mp4` 로 두면 별도 수정 없이 바로 재생됩니다.
  (다른 이름이면 `index.html` 의 `<source src="assets/updates/clip-1.mp4">` 경로를 맞춰주세요.)
- 영상이 아직 없으면 `poster` 이미지(현재 slide-3.jpg)가 대신 표시됩니다.
- 권장: mp4(H.264), 16:9, 10MB 이하.
